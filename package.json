{
  "name": "writing-agent-benchmark",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A reproducible benchmark contract and evaluation workflow for writing agents.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate.mjs .",
    "privacy:scan": "node scripts/privacy-scan.mjs .",
    "check": "npm test && npm run validate && npm run privacy:scan"
  },
  "engines": {"node": ">=20"},
  "license": "MIT"
}
